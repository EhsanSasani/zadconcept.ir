# گزارش QA پچ نهایی تصویر ZAD

## علت فنی

در نسخهٔ قبلی، فرم استاندارد `ImageField` جنگو می‌توانست پیش از رسیدن فایل به
تابع تبدیل، براساس نام/پسوند آن را رد کند. همچنین افزودن رشتهٔ `HEIC` به
whitelist درمان واقعی نبود؛ پلاگین رسمی `pillow-heif` هر دو پسوند `.heic` و
`.heif` را با فرمت داخلی `HEIF` گزارش می‌کند.

پچ نهایی در پنل ادمین از یک FileField محدود به همان UI تصویر استفاده می‌کند تا
تمام اعتبارسنجی محتوایی فقط در Pipeline انجام شود. پس از Decode کامل، مدل جنگو
فقط یک فایل معتبر `.webp` می‌بیند و validator معمول مدل همچنان فعال است.

## سناریوهای پوشش‌داده‌شده

| سناریو | نتیجه |
|---|---|
| `.jpg` و `.jpeg` عادی و progressive | تبدیل موفق به WebP |
| پسوند با حروف بزرگ | تبدیل موفق |
| MIME اشتباه یا `application/octet-stream` | تشخیص از محتوای واقعی |
| JPEG با نام `.heic` و HEIF با نام `.jpeg` | تبدیل موفق |
| HEIC و HEIF واقعی | تبدیل موفق |
| HEIFهای ۸، ۱۰ و ۱۲ بیت | تبدیل موفق |
| HEIF آلفادار، burst، spatial و دارای auxiliary HDR | فریم اصلی به WebP |
| JPEG موبایلی از نوع MPO با نام `.jpg` | فریم اصلی به WebP |
| JPEG ادیت‌شده با رنگ CMYK | تبدیل به RGB WebP |
| EXIF Orientation | چرخش صحیح پیش از Resize |
| ICC معتبر | حفظ در خروجی |
| ICC خراب | حذف امن بدون 500 |
| EXIF/XMP/GPS | حذف از خروجی |
| رکورد بدون عکس جدید | عکس قبلی بدون Re-encode حفظ می‌شود |
| حذف عکس از checkbox | رفتار قبلی حفظ می‌شود |
| GIF/APNG/WebP/AVIF متحرک | رد با پیام فرم |
| فایل خراب یا غیرتصویری | رد با پیام فرم |
| بیشتر از ۲۰٬۰۰۰٬۰۰۰ بایت | رد پیش از Decode |
| بیشتر از ۶۰ مگاپیکسل | رد پیش از Decode کامل |

## شواهد اجرا

- محیط سازگاری نهایی: Python 3.12، Django 6.0.7، Pillow 12.1.1 (نسخهٔ
  فعلی سرور) و pillow-heif 1.5.0.
- ۳۰ تست مرتبط با Pipeline، فرم‌های ادمین و تست قدیمی پروژه: پاس.
- HTTP POST واقعی به صفحهٔ افزودن «گل ارسال روز» با HEIF: ذخیرهٔ موفق WebP.
- تمام ImageFieldهای ثبت‌شده در Django Admin: متصل به فیلد جدید Pipeline.
- ۳۶ فایل معتبر از corpus رسمی `pillow-heif` 1.5.0: همگی پاس.
- ۳ فایل خراب رسمی HEIF: همگی رد.
- ۳ فایل MPO از corpus رسمی Pillow: همگی پاس.
- فایل HEIC دویست‌مگاپیکسلی: رد ایمن با پیام محدودیت ابعاد.
- `manage.py check`: پاس.
- `makemigrations --check --dry-run`: بدون تغییر.
- بررسی Syntax و Ruff برای خطاهای Import/Fatal در سه فایل پچ: پاس.

کل مجموعهٔ ۱۰۷ تست snapshot موجود پروژه نیز اجرا شد؛ ۱۱ Failure و ۲ Error
قدیمی و خارج از این پچ باقی ماند (فایل ناقص تنظیمات Nginx در stage، قراردادهای
قدیمی SEO/template و ناسازگاری تست قدیمی wedding). تمام ۳۰ تست مرتبط با این
پچ جداگانه پاس شدند.

## موارد عمداً خارج از پچ

- فایل Media قبلی هنگام جایگزینی خودکار حذف نمی‌شود؛ این تصمیم برای جلوگیری
  از حذف ناخواسته و حفظ امکان بازیابی است.
- ساخت خودکار variantهای `520w/1040w/1600w` در این پچ اضافه نشده و رفتار فعلی
  فرمان `optimize_images` دست‌نخورده مانده است.
- Nginx تغییر نمی‌کند و درخواست بزرگ‌تر از سقف آن ممکن است پیش از Django با
  `413` رد شود.
